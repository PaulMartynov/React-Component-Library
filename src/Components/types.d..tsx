interface HeaderProps {
  level: number;
  children: JSX.Element | string;
}

interface ParagraphProps {
  styles: string[];
  children: JSX.Element | string;
}

interface SpaceBlockProps {
  blockHeight: number;
}
